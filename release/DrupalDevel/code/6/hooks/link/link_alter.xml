<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>link_alter</title>
		<external>http://api.drupal.org/api/search/7/hook_link_alter</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implementation of hook_link_alter();
*/
function ${file_name}_link_alter(&$links, $node, $comment = NULL) {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_link_alter(&$links, $node, $comment = NULL)</h2>

<p>Perform alterations before links on a node or comment are rendered.</p>
<p>One popular use of this hook is to modify/remove links from other modules.
If you want to add a link to the links section of a node or comment, use
hook_link() instead.</p>

<h3>Parameters</h3>
<p><b>
$links</b> -- 
Nested array of links for the node or comment keyed by providing module.</p>
<p><b>$node</b> -- 
A node object.</p>
<p><b>$comment</b> -- 
An optional comment object if the links are comment links. If not
provided, the links are node links.</p>



]]></help>
	</item>