<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>prepare</title>
		<external>http://api.drupal.org/api/search/7/hook_prepare</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implementation of hook_prepare();
*/
function ${file_name}_prepare(&$node) {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_prepare(&$node)</h2>

<p>This is a hook used by node modules. It is called after load but before the
node is shown on the add/edit form.</p>

<h3>Parameters</h3>
<p>
&amp;$node
The node being saved.</p>



]]></help>
	</item>