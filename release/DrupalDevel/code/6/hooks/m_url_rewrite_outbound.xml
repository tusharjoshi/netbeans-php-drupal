<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>m_url_rewrite_outbound</title>
		<external>http://api.drupal.org/api/search/7/hook_m_url_rewrite_outbound</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implementation of <? print $hook ?>().
*/
function ${file_name}_<? print $signature ?> {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>custom_url_rewrite_outbound(&$path, &$options, $original_path)</h2>

<p>custom_url_rewrite_outbound is not a hook, it's a function you can add to
settings.php to alter all links generated by Drupal. This function is called from url().
This function is called very frequently (100+ times per page) so performance is
critical.</p>
<p>This function should change the value of $path and $options by reference.</p>

<h3>Parameters</h3>
<p><b>
$path</b> -- 
The alias of the $original_path as defined in the database.
If there is no match in the database it'll be the same as $original_path</p>
<p><b>$options</b> -- 
An array of link attributes such as querystring and fragment. See url().</p>
<p><b>$original_path</b> -- 
The unaliased Drupal path that is being linked to.</p>



]]></help>
	</item>