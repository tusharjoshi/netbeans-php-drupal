<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>comment_view_alter</title>
		<external>http://api.drupal.org/api/search/7/hook_comment_view_alter</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_comment_view_alter();
*/
function ${file_name}_comment_view_alter(&$build) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_comment_view_alter(&$build)</h2>

<p>The comment was built; the module may modify the structured content.</p>
<p>This hook is called after the content has been assembled in a structured array
and may be used for doing processing which requires that the complete comment
content structure has been built.</p>
<p>If the module wishes to act on the rendered HTML of the comment rather than the
structured content array, it may use this hook to add a #post_render callback.
Alternatively, it could also implement hook_preprocess_comment(). See
drupal_render() and theme() documentation respectively for details.</p>

<h3>Parameters</h3>
<p><b>
$build</b> -- 
A renderable array representing the comment.</p>



]]></help>
	</item>