<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>block_configure</title>
		<external>http://api.drupal.org/api/search/7/hook_block_configure</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_block_configure();
*/
function ${file_name}_block_configure($delta = '') {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_block_configure($delta = '')</h2>

<p>Define a configuration form for a block.</p>

<h3>Parameters</h3>
<p><b>
$delta</b> -- 
Which block is being configured. This is a unique identifier for the block
within the module, defined in hook_block_info().</p>



]]></help>
	</item>