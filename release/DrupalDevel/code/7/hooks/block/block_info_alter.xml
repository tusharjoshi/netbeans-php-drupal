<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>block_info_alter</title>
		<external>http://api.drupal.org/api/search/7/hook_block_info_alter</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_block_info_alter().
*/
function ${file_name}_block_info_alter(&$blocks, $theme, $code_blocks) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_block_info_alter(&$blocks, $theme, $code_blocks)</h2>

<p>Change block definition before saving to the database.</p>

<h3>Parameters</h3>
<p><b>
$blocks</b> -- 
A multidimensional array of blocks keyed by the defining module and delta;
the values are blocks returned by hook_block_info(). This hook is fired
after the blocks are collected from hook_block_info() and the database,
right before saving back to the database.</p>
<p><b>$theme</b> -- 
The theme these blocks belong to.</p>
<p><b>$code_blocks</b> -- 
The blocks as defined in hook_block_info() before being overwritten by the
database data.</p>



]]></help>
	</item>