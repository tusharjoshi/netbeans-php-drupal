<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>modules_disabled</title>
		<external>http://api.drupal.org/api/search/7/hook_modules_disabled</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_modules_disabled();
*/
function ${file_name}_modules_disabled($modules) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_modules_disabled($modules)</h2>

<p>Perform necessary actions after modules are disabled.</p>
<p>This function differs from hook_disable() in that it gives all other modules
a chance to perform actions when modules are disabled, whereas hook_disable()
is only called on the module actually being disabled.</p>

<h3>Parameters</h3>
<p><b>
$modules</b> -- 
An array of the modules that were disabled.</p>



]]></help>
	</item>