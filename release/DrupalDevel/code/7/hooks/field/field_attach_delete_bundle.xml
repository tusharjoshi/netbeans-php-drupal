<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>field_attach_delete_bundle</title>
		<external>http://api.drupal.org/api/search/7/hook_field_attach_delete_bundle</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_field_attach_delete_bundle().
*/
function ${file_name}_field_attach_delete_bundle($entity_type, $bundle, $instances) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_field_attach_delete_bundle($entity_type, $bundle, $instances)</h2>

<p>Act on field_attach_delete_bundle.</p>
<p>This hook is invoked after the field module has performed the operation.</p>

<h3>Parameters</h3>
<p><b>
$entity_type</b> -- 
The type of entity; for example, 'node' or 'user'.</p>
<p><b>$bundle</b> -- 
The bundle that was just deleted.</p>
<p><b>$instances</b> -- 
An array of all instances that existed for the bundle before it was
deleted.</p>



]]></help>
	</item>