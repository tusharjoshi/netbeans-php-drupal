<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>field_create_instance</title>
		<external>http://api.drupal.org/api/search/7/hook_field_create_instance</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_field_create_instance();
*/
function ${file_name}_field_create_instance($instance) {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_field_create_instance($instance)</h2>

<p>Act on a field instance being created.</p>
<p>This hook is invoked from field_create_instance() after the instance record
is saved, so it cannot be used to modify the instance itself.</p>

<h3>Parameters</h3>
<p><b>
$instance</b> -- 
The instance just created.</p>



]]></help>
	</item>