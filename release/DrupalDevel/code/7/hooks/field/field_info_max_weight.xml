<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>field_info_max_weight</title>
		<external>http://api.drupal.org/api/search/7/hook_field_info_max_weight</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_field_info_max_weight();
*/
function ${file_name}_field_info_max_weight($entity_type, $bundle, $context) {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_field_info_max_weight($entity_type, $bundle, $context)</h2>

<p>Returns the maximum weight for the entity components handled by the module.</p>
<p>Field API takes care of fields and 'extra_fields'. This hook is intended for
third-party modules adding other entity components (e.g. field_group).</p>

<h3>Parameters</h3>
<p><b>
$entity_type</b> -- 
The type of entity; e.g. 'node' or 'user'.</p>
<p><b>$bundle</b> -- 
The bundle name.</p>
<p><b>$context</b> -- 
The context for which the maximum weight is requested. Either 'form', or
the name of a view mode.</p>



]]></help>
	</item>