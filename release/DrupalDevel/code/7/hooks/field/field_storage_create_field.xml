<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>field_storage_create_field</title>
		<external>http://api.drupal.org/api/search/7/hook_field_storage_create_field</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_field_storage_create_field().
*/
function ${file_name}_field_storage_create_field($field) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_field_storage_create_field($field)</h2>

<p>Act on creation of a new field.</p>
<p>This hook is invoked from field_create_field() to ask the field storage
module to save field information and prepare for storing field instances.
If there is a problem, the field storage module should throw an exception.</p>

<h3>Parameters</h3>
<p><b>
$field</b> -- 
The field structure being created.</p>



]]></help>
	</item>