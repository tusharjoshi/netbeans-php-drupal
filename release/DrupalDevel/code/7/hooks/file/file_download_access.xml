<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>file_download_access</title>
		<external>http://api.drupal.org/api/search/7/hook_file_download_access</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_file_download_access();
*/
function ${file_name}_file_download_access($field, $entity_type, $entity) {
   ${set_cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_file_download_access($field, $entity_type, $entity)</h2>

<p>Control download access to files.</p>
<p>The hook is typically implemented to limit access based on the entity the
file is referenced, e.g., only users with access to a node should be allowed
to download files attached to that node.</p>

<h3>Parameters</h3>
<p><b>
$field</b> -- 
The field to which the file belongs.</p>
<p><b>$entity_type</b> -- 
The type of $entity; for example, 'node' or 'user'.</p>
<p><b>$entity</b> -- 
The $entity to which $file is referenced.</p>



]]></help>
	</item>