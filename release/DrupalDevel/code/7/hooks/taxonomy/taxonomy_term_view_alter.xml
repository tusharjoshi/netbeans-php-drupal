<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>taxonomy_term_view_alter</title>
		<external>http://api.drupal.org/api/search/7/hook_taxonomy_term_view_alter</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_taxonomy_term_view_alter().
*/
function ${file_name}_taxonomy_term_view_alter(&$build) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_taxonomy_term_view_alter(&$build)</h2>

<p>Alter the results of taxonomy_term_view().</p>
<p>This hook is called after the content has been assembled in a structured
array and may be used for doing processing which requires that the complete
taxonomy term content structure has been built.</p>
<p>If the module wishes to act on the rendered HTML of the term rather than the
structured content array, it may use this hook to add a #post_render
callback. Alternatively, it could also implement
hook_preprocess_taxonomy_term(). See drupal_render() and theme()
documentation respectively for details.</p>

<h3>Parameters</h3>
<p><b>
$build</b> -- 
A renderable array representing the node content.</p>



]]></help>
	</item>