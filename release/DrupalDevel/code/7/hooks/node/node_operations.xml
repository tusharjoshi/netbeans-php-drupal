<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>node_operations</title>
		<external>http://api.drupal.org/api/search/7/hook_node_operations</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_node_operations();
*/
function ${file_name}_node_operations() {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_node_operations()</h2>

<p>Add mass node operations.</p>
<p>This hook enables modules to inject custom operations into the mass
operations dropdown found at admin/content, by associating a callback
function with the operation, which is called when the form is submitted. The
callback function receives one initial argument, which is an array of the
checked nodes.</p>




]]></help>
	</item>