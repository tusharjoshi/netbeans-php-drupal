<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>node_revision_delete</title>
		<external>http://api.drupal.org/api/search/7/hook_node_revision_delete</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_node_revision_delete();
*/
function ${file_name}_node_revision_delete($node) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_node_revision_delete($node)</h2>

<p>Respond to deletion of a node revision.</p>
<p>This hook is invoked from node_revision_delete() after the revision has been
removed from the node_revision table, and before
field_attach_delete_revision() is called.</p>

<h3>Parameters</h3>
<p><b>
$node</b> -- 
The node revision (node object) that is being deleted.</p>



]]></help>
	</item>