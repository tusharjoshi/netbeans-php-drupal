<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>prepare</title>
		<external>http://api.drupal.org/api/search/7/hook_prepare</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_prepare().
*/
function ${file_name}_prepare($node) {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_prepare($node)</h2>

<p>Act on a node object about to be shown on the add/edit form.</p>
<p>This hook is invoked only on the module that defines the node's content type
(use hook_node_prepare() to act on all node preparations).</p>
<p>This hook is invoked from node_object_prepare() before the general
hook_node_prepare() is invoked.</p>

<h3>Parameters</h3>
<p><b>
$node</b> -- 
The node that is about to be shown on the add/edit form.</p>



]]></help>
	</item>