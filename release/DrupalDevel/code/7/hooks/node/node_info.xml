<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>node_info</title>
		<external>http://api.drupal.org/api/search/7/hook_node_info</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_node_info().
*/
function ${file_name}_node_info() {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_node_info()</h2>

<p>Define module-provided node types.</p>
<p>This hook allows a module to define one or more of its own node types. For
example, the blog module uses it to define a blog node-type named "Blog
entry." The name and attributes of each desired node type are specified in
an array returned by the hook.</p>
<p>Only module-provided node types should be defined through this hook. User-
provided (or 'custom') node types should be defined only in the 'node_type'
database table, and should be maintained by using the node_type_save() and
node_type_delete() functions.</p>




]]></help>
	</item>