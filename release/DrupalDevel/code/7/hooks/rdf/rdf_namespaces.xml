<?xml version="1.0" encoding="UTF-8"?>
	<item>
		<title>rdf_namespaces</title>
		<external>http://api.drupal.org/api/search/7/hook_rdf_namespaces</external>
		<group>hook</group>
		<template><![CDATA[
/**
* Implements hook_rdf_namespaces().
*/
function ${file_name}_rdf_namespaces() {
   ${cursor}
}
]]></template>
		<help><![CDATA[<h2>hook_rdf_namespaces()</h2>

<p>Allow modules to define namespaces for RDF mappings.</p>
<p>Many common namespace prefixes are defined in rdf_rdf_namespaces(). However,
if a module implements hook_rdf_mapping() and uses a prefix that is not
defined in rdf_rdf_namespaces(), this hook should be used to define the new
namespace prefix.</p>




]]></help>
	</item>